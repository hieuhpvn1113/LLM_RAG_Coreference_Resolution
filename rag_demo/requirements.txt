# LLM — OpenAI-compatible local
openai>=1.30.0

# Vector DB (dense + sparse BM25 hybrid)
qdrant-client>=1.9.0
fastembed>=0.3.0          # sparse BM25 model cho Qdrant

# Graph DB
neo4j>=5.18.0

# PostgreSQL
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Embedding & NLP (local)
sentence-transformers>=3.0.0
nltk>=3.8.0
tiktoken>=0.7.0

# Core
numpy>=1.26.0
pydantic>=2.0.0
python-dotenv>=1.0.0
chardet>=5.0.0

# File parsing — PDF / DOCX / XLSX / PPTX / HTML / MD
kreuzberg>=0.1.0

# API (optional)
fastapi>=0.111.0
uvicorn>=0.29.0

# RAG evaluation (LLM-as-judge) - pinned compatible set
ragas==0.2.12
datasets>=2.19.0
pandas>=2.2.0
langchain-openai==0.2.6
langchain-community==0.3.7
langchain-core==0.3.17
